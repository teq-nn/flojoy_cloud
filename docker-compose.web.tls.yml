services:
  web_build:
    image: oven/bun:1
    container_name: flojoy_web_build
    working_dir: /repo
    command: sh -lc "bun install && cd apps/web && bun run build"
    volumes:
      - ./:/repo
    env_file:
      - ./apps/web/.env
    restart: "no"

  web:
    image: nginx:alpine
    container_name: flojoy_web
    depends_on:
      web_build:
        condition: service_completed_successfully
    volumes:
      - ./apps/web/dist:/usr/share/nginx/html:ro
      - ./apps/web/nginx.letsencrypt.conf:/etc/nginx/http.d/default.conf:ro
      - ./nginx/letsencrypt:/etc/letsencrypt:ro
      - ./nginx/certbot/www:/var/www/certbot:ro
    # Terminate TLS at Nginx Proxy Manager; publish only HTTP on 5173
    ports:
      - "5173:80"
    restart: unless-stopped
  certbot:
    image: certbot/certbot:latest
    container_name: flojoy_certbot
    volumes:
      - ./nginx/letsencrypt:/etc/letsencrypt
      - ./nginx/certbot/www:/var/www/certbot
    entrypoint: ["/bin/sh", "-c"]
    command: "sleep infinity"
